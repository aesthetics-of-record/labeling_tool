"use client";

import { useEffect, useState } from "react";

const Tool = () => {
  const [totalPage, setTotalPage] = useState(1);
  const [activePage, setPage] = useState(1);

  useEffect(() => {}, []);
  return <div>이미지 홈 페이지</div>;
};

export default Tool;
